We've had multiple sessions of template edits that broke Django templates (split {% block %}, {% else %}, {% endfor %} tags across lines). I need a full systematic audit and fix — not another one-off patch. Do this in order:

STEP 1 — Template Tag Integrity Audit
Scan every .html file in the project for:
- Unclosed or mismatched {% block %} / {% endblock %}
- Unclosed or mismatched {% if %} / {% else %} / {% endif %}
- Unclosed or mismatched {% for %} / {% endfor %}
- Any Django template tag split across multiple lines with stray text/whitespace inside the tag delimiters
- Orphaned tags (endfor/endif/endblock with no matching opener) — you found one already in individual_assessment_complete_backup.html, check if that file is even used anywhere or if it's dead code to delete
List every file with issues and the exact line numbers BEFORE making any edits. Do not fix yet.

STEP 2 — "No job titles available for assessment" bug
This means the dashboard view's queryset for job titles is returning empty. Check:
- The view function that renders analysis/individual_dashboard.html — what queryset/filter populates job titles
- Whether this is a real empty DB table, a broken foreign key filter, a user-scoping issue (e.g. filtering by request.user when the job titles should be global), or an exception being silently swallowed
- Print/log the actual queryset count before it hits the template
Report root cause before fixing.

STEP 3 — Login/session not persisting (forced to create new account every time)
This is NOT normal Django behavior — sessions should persist via cookies. Check:
- SESSION_ENGINE, SESSION_COOKIE_AGE, SESSION_COOKIE_SECURE, SESSION_COOKIE_SAMESITE in settings.py
- Whether DEBUG/ALLOWED_HOSTS changes or a recent settings.py edit affected cookie domain
- Whether the DB is being reset/migrated on every restart (SQLite file getting wiped, or using an in-memory/ephemeral DB on Replit which loses data on redeploy)
- Whether Replit's environment resets storage on every run (this is common — check if using a persistent DB path or Replit's persistent storage, not a temp/ephemeral disk)
Report root cause before fixing — this is likely a Replit storage/DB persistence issue, not a Django bug.

STEP 4 — Apply fixes
Only after reporting all three root causes above, apply fixes one at a time, and after EACH fix, run the actual affected page/flow and paste real console/terminal output